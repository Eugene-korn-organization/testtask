name: Drupal PHP and JS code standards checker

on: pull_request

jobs:
  phpcs-checker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3        
         
      - name: Checking php Drupal standards
        run: |
          ls -la
          cd docker4drupal
          ls -la
          ./vendor/bin/phpcs -i
          ./vendor/bin/phpcs --standard=Drupal,DrupalPractice web/modules/custom
